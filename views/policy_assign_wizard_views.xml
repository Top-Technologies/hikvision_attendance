<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hikvision_policy_assign_wizard_form" model="ir.ui.view">
        <field name="name">hikvision.policy.assign.wizard.form</field>
        <field name="model">hikvision.policy.assign.wizard</field>
        <field name="arch" type="xml">
            <form string="Assign Attendance Policy">
                <group>
                    <field name="policy_id"/>
                    <field name="employee_ids" widget="many2many_tags" invisible="1"/>
                    <div class="text-muted">
                        Assign the selected policy to all selected employees.
                    </div>
                </group>
                <footer>
                    <button string="Assign" name="action_assign" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_hikvision_policy_assign_wizard" model="ir.actions.act_window">
        <field name="name">Assign Attendance Policy</field>
        <field name="res_model">hikvision.policy.assign.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="hr.model_hr_employee"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
